# Обновление системы: Упрощённая авторизация и улучшенное сохранение

## Изменения

### 1. Упрощённая система авторизации ✅
- **Убрали email + подтверждение** → Теперь только **никнейм + пароль**
- Регистрация: вводите никнейм (3-20 символов), пароль (минимум 6), подтверждаете пароль
- Вход: никнейм + пароль
- Никнейм уникальный для каждого пользователя
- Сессия хранится в localStorage (действует 30 дней)

### 2. Исправлена бесконечная загрузка при обновлении страницы ✅
- Сессия пользователя теперь сохраняется в localStorage
- При перезагрузке страницы сессия автоматически восстанавливается
- Нет необходимости заново входить в систему

### 3. Автосохранение каждую минуту ✅
- Изменён интервал автосохранения: с 30 секунд → на 60 секунд (1 минута)
- Автосохранение работает только для авторизованных пользователей
- Прогресс сохраняется в облако Supabase

### 4. Кнопка "Сохранить прогресс" на странице игры ✅
- Добавлена кнопка для ручного сохранения прогресса
- Показывает статус сохранения: "Сохранение...", "Сохранено!", "Ошибка!"
- Доступна только для авторизованных пользователей

### 5. Предупреждение при закрытии вкладки ✅
- При попытке закрыть вкладку/браузер появляется диалоговое окно
- Текст: "Вы уверены? Не забудьте сохранить прогресс!"
- Перед закрытием автоматически сохраняет прогресс

## Технические детали

### Изменённые файлы:
1. `src/context/AuthContext.tsx` - новая система авторизации
2. `src/pages/Login/Login.tsx` - форма входа/регистрации
3. `src/pages/Profile/Profile.tsx` - профиль пользователя
4. `src/components/CloudSave/CloudSave.tsx` - автосохранение и предупреждения
5. `src/pages/Game/Game.tsx` - кнопка сохранения
6. `src/pages/Game/Game.css` - стили для кнопки

### Новые файлы:
1. `database_migration.sql` - SQL скрипт для миграции БД
2. `MIGRATION_GUIDE.md` - инструкция по миграции

## База данных

### Новая структура:

```sql
-- Таблица пользователей
users (
  id UUID PRIMARY KEY,
  nickname VARCHAR(20) UNIQUE NOT NULL,
  password_hash VARCHAR(64) NOT NULL,
  display_name VARCHAR(50),
  created_at TIMESTAMPTZ,
  updated_at TIMESTAMPTZ
)

-- Таблица профилей (связана с users)
profiles (
  user_id UUID REFERENCES users(id),
  nickname VARCHAR(20),
  display_name VARCHAR(50),
  avatar_url TEXT,
  bio TEXT,
  is_profile_complete BOOLEAN
)

-- Таблица сохранений (связана с users)
saves (
  user_id UUID REFERENCES users(id),
  save_data JSONB,
  created_at TIMESTAMPTZ,
  updated_at TIMESTAMPTZ
)
```

## Как применить изменения?

### 1. Миграция базы данных

**Через Supabase Dashboard:**
1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Перейдите в SQL Editor
3. Скопируйте содержимое `database_migration.sql`
4. Выполните скрипт

**Или через psql:**
```bash
psql -h <your-project-ref>.supabase.co -U postgres -d postgres -f database_migration.sql
```

### 2. Код уже обновлён!
Все изменения в коде уже сделаны, просто протестируйте приложение.

## Тестирование

1. **Регистрация:**
   - Перейдите на страницу логина
   - Нажмите "Зарегистрироваться"
   - Введите никнейм (например, "TestUser123")
   - Введите пароль (минимум 6 символов)
   - Подтвердите пароль
   - Нажмите "Зарегистрироваться"

2. **Вход:**
   - Введите никнейм
   - Введите пароль
   - Нажмите "Войти"

3. **Проверка сохранения:**
   - Поиграйте немного
   - Нажмите кнопку "💾 Сохранить прогресс"
   - Обновите страницу (F5)
   - Убедитесь, что прогресс сохранился

4. **Проверка предупреждения:**
   - Попробуйте закрыть вкладку
   - Должно появиться предупреждение

## Что дальше?

Система готова к использованию! Все основные функции работают:
- ✅ Простая регистрация и вход
- ✅ Автоматическое сохранение сессии
- ✅ Автосохранение каждую минуту
- ✅ Ручное сохранение кнопкой
- ✅ Предупреждение при закрытии
- ✅ Сохранение прогресса в облако Supabase

## Примечания

⚠️ **Важно:** После миграции БД все старые пользователи будут потеряны, так как мы перешли с email-based auth на nickname-based. Это нормально для dev-среды.

🔐 **Безопасность:** В текущей реализации пароли хешируются SHA-256 на клиенте. Для production рекомендуется использовать bcrypt на серверной стороне.

💾 **Хранение:** Сессии хранятся в localStorage и действуют 30 дней. Пользователь останется авторизованным даже после закрытия браузера.
